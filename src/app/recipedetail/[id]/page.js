'use client'
import React, { useEffect, useState } from 'react';

const Details = ({params}) => {
    let {name}=params
    
    return (
        <div>
            
        </div>
    );
};

export default Details;